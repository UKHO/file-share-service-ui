import { FilterOperatorPipe } from './filter-operator.pipe';

describe('FilterOperatorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOperatorPipe();
    expect(pipe).toBeTruthy();
  });
});
